name: pdist_aggregation
channels:
  - conda-forge
dependencies:
  - jupyter
  - numpy
  - cython
  - matplotlib
  - seaborn
  - pandas
  - scikit-learn-intelex
  - pip
  - pip:
      - --pre
      - --index-url https://pypi.anaconda.org/scipy-wheels-nightly/simple
      - scikit-learn
      # Build and install pdist_aggregation from source
      - -e src -v